#include<bits/stdc++.h>
using namespace std;
int main(){
    int arr[3],midpoint,sum=0,size=0;
    scanf("%d%d%d",&arr[0],&arr[1],&arr[2]);
    midpoint = (arr[0]+arr[1]+arr[2])/3;
    size = sizeof(arr)/sizeof(arr[0]);
    sort(arr,arr+size);
    sum = ()

    return 0;
}
